import styled from 'styled-components';
import Loader from 'calcite-react/Loader';

const StyledSidebar = styled.div`
  height: 100%;
  width: 100%;
`;

const StyledLoader = styled(Loader)`
  margin-top: 4rem;
`;

export { StyledSidebar, StyledLoader };
