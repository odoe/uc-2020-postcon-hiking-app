// Framework and third-party non-ui
import React from 'react';

// App components

// JSON & Styles
import { StyledTrailCard } from './TrailCard-styled';

// Third-party components (buttons, icons, etc.)

const TrailCard = () => {
  return <StyledTrailCard data-testid="TrailCard">TrailCard</StyledTrailCard>;
};

export default TrailCard;
