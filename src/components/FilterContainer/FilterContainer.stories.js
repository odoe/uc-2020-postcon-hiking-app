import React from 'react';
import FilterContainer from '.';

export default {
  title: 'FilterContainer',
  component: FilterContainer,
};

export const Default = () => <FilterContainer>FilterContainer</FilterContainer>;
