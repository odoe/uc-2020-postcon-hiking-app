// Framework and third-party non-ui
import React from 'react';

// App components

// JSON & Styles
import { StyledTrailsList } from './TrailsList-styled';

// Third-party components (buttons, icons, etc.)

const TrailsList = () => {
  return (
    <StyledTrailsList data-testid="TrailsList">TrailsList</StyledTrailsList>
  );
};

export default TrailsList;
